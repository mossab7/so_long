        rak 3arf ax dir:

        if(game->map.map[game->enemy[i].y_end_pos / SCALE][game->enemy[i].x_end_pos / SCALE] == 'C')
        {
            game->collectable.x_pos = game->enemy[i].x_end_pos;
            game->collectable.y_pos = game->enemy[i].y_end_pos;
            draw_sprite_to_canvas(game, game->collectable);
            return;
        }

        we could do a screen shot to the backgound of character and re draw it when the character move 